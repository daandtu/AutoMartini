name: automartiniM3

channels:
  - conda-forge

dependencies:
  - python>=3.8
  - pip
  # Build dependencies for Cython extension
  - numpy>=1.23.1
  - cython>=3.0
  # Runtime dependencies (also in pyproject.toml)
  - beautifulsoup4>=4.11.1
  - lxml>=4.9.1
  - requests>=2.28.1
  - six>=1.16.0
  - rdkit>=2022.03.4
  # Development
  - pytest
  - pip:
      - -e .
